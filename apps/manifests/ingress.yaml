apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apps-ingress

spec:
  rules:
    - http:
        paths:
          - path: /pingpong
            pathType: Prefix
            backend:
              service:
                name: pingpong-svc
                port:
                  number: 2345

          - path: /status
            pathType: Prefix
            backend:
              service:
                name: log-svc
                port:
                  number: 2345