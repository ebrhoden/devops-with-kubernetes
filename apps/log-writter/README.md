# Part of log application

## What it does
Generates a random string on startup and writes a line with the random string and timestamp every 5 seconds into a file located at /shared/log.txt.

## Source Code
The `Dockerfile` and `log_writter.py` files are the source code for the Docker image `ebrhoden/log_writter:0.0.1`.
