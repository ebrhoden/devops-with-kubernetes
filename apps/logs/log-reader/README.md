# Part of log application

## What it does
Reads the file at /shared/log.txt and provides the content in the HTTP GET endpoint for the user to see

## Source Code
The `Dockerfile` and `log_reader.py` files are the source code for the Docker image `ebrhoden/log_reader:0.0.1`.
